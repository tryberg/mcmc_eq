This example uses the mcmc_eq code in "JHD" mode where the velocity model is input from the file model.dat and remains fixed. Synthetic station corrections are added along with pick noise.

Run the code on your cluster to get the rjx*out files, then run all the various post processing and plotting scripts via:

runAllonOutputs.sh 175000 100
